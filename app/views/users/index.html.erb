<% provide(:title, 'すべてのユーザー')%>
<h1>All Users</h1>

<div class="jumbotron">
  <%= will_paginate @users, :previous_label => '&lt 前へ', :next_label => '次へ &gt' %>

  <ul class="users">
    <%= render @users %>
  </ul>

  <%= will_paginate @users, :previous_label => '&lt 前へ', :next_label => '次へ &gt' %>
</div>